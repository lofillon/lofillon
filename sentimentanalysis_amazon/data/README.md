## Data directory

- `raw/`: put the downloaded Kaggle CSV here as `french_to_english_product.csv`
- `processed/`: optional intermediate datasets/features (not committed)

This project intentionally does **not** commit the dataset.

